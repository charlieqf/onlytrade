# Tests

## Unit tests

- Co-located with source as `src/**/*.test.ts` and `src/**/*.test.tsx`
- Runner: Vitest (`vitest.config.ts`)

## Integration / E2E (planned)

This project will likely use Playwright for E2E.

- `tests/e2e/` - browser-level specs
- `tests/fixtures/` - mock API payloads
